---
phase: 01-code-review-and-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/overlay_name.rs
  - src/fuzzy.rs
  - src/json_merge.rs
  - src/github.rs
  - src/upstream.rs
  - src/reference.rs
  - src/state.rs
  - src/config.rs
  - src/cache.rs
  - src/sources.rs
  - src/overlay_repo.rs
  - src/detection.rs
  - src/selection.rs
autonomous: true
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-03
  - REVIEW-04
  - REVIEW-07

must_haves:
  truths:
    - "All 13 non-orchestration source modules have been reviewed for correctness"
    - "No silent error-swallowing patterns exist in reviewed modules"
    - "Security-sensitive areas (git command construction, path validation, URL scheme validation) have been verified safe"
    - "SourceResolver trait implementation in state.rs is complete -- all 3 OverlaySource variants handled in every method"
    - "Every bug found during review is either fixed inline or documented as a finding for FIX-01"
  artifacts:
    - path: ".planning/phases/01-code-review-and-bug-fixes/01-01-SUMMARY.md"
      provides: "Review findings, bugs found/fixed, documented concerns"
  key_links:
    - from: "src/state.rs"
      to: "SourceResolver trait"
      via: "All match arms on OverlaySource cover Local, GitHub, OverlayRepo"
      pattern: "fn is_mutable|fn is_syncable|fn is_updatable|fn source_type_label"
    - from: "src/cache.rs"
      to: "git commands"
      via: "Command::new(\"git\") argument construction"
      pattern: "Command::new.*git"
---

<objective>
Review all 13 non-orchestration source modules for correctness, following dependency-depth ordering: leaf modules first, then infrastructure, then resolution and support modules.

Purpose: Establish foundational correctness before reviewing the large orchestration modules (lib.rs, cli.rs) in Plan 02. Identify and fix any bugs found during review. Verify SourceResolver trait completeness.

Output: Review findings documented in SUMMARY. Any correctness bugs fixed inline. Tech debt and improvement opportunities documented but NOT acted on (per project constraints).
</objective>

<execution_context>
@/home/tylerbu/.claude/get-shit-done/workflows/execute-plan.md
@/home/tylerbu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-review-and-bug-fixes/01-RESEARCH.md
@ARCHITECTURE.md
@DEV.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review leaf modules for correctness</name>
  <files>
    src/overlay_name.rs
    src/fuzzy.rs
    src/json_merge.rs
    src/github.rs
    src/upstream.rs
    src/reference.rs
  </files>
  <action>
Review each leaf module systematically using this checklist. These modules have zero internal dependencies so can be reviewed independently.

**For each module, check:**
1. **Correctness**: Does each function do what its name/docs say?
2. **Error handling**: Are errors propagated with `.context()`? Any silent failures (ignored Results)?
3. **Edge cases**: Empty inputs, Unicode, very long strings, path separators
4. **Security**: Command injection, path traversal, flag injection
5. **Match exhaustiveness**: All enum variants handled? Check for `..` rest patterns hiding fields
6. **Panic potential**: Any `.unwrap()` or `.expect()` on user-supplied data that could panic?
7. **Resource cleanup**: File handles, temp dirs properly cleaned up?
8. **Test coverage**: Do existing tests cover critical paths?

**Module-specific focus areas:**

- `overlay_name.rs` (95 lines): Unicode edge cases in name validation. What characters are accepted/rejected?
- `fuzzy.rs` (270 lines): Scoring correctness with empty inputs, single-character inputs, exact matches
- `json_merge.rs` (250 lines): Type mismatch handling during deep merge. What happens with null values? Array vs object merge behavior
- `github.rs` (706 lines): Flag injection check at line 154 (`if s.starts_with('-')`). 40-char hex ambiguity between commit hashes and branch names. URL format edge cases
- `upstream.rs` (277 lines): Git remote parsing for both HTTPS and SSH formats. Edge cases with unusual remote configurations
- `reference.rs` (409 lines): Input parsing completeness. Ambiguous references (could be local path OR repo reference). Does it handle Windows paths correctly?

**IMPORTANT constraints:**
- Only fix correctness bugs (wrong behavior, crashes, security issues)
- Do NOT refactor code that works correctly but could be structured better
- Document tech debt / improvement opportunities in the SUMMARY, do not fix them
- If you find a bug, fix it inline and note it in SUMMARY under "Bugs Fixed"
  </action>
  <verify>
    <automated>cargo test --lib -- overlay_name fuzzy json_merge github upstream reference 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 leaf modules reviewed. Bugs found are fixed with tests passing. Findings (correctness issues, edge cases, tech debt) documented.</done>
</task>

<task type="auto">
  <name>Task 2: Review infrastructure and resolution/support modules plus verify SourceResolver</name>
  <files>
    src/state.rs
    src/config.rs
    src/cache.rs
    src/sources.rs
    src/overlay_repo.rs
    src/detection.rs
    src/selection.rs
  </files>
  <action>
Review each infrastructure/resolution/support module using the same checklist from Task 1, with additional module-specific focus areas.

**Module-specific focus areas:**

- `state.rs` (1656 lines): **SourceResolver trait verification is CRITICAL (REVIEW-07)**. The SourceResolver trait (lines ~242-314) must handle all 3 OverlaySource variants (Local, GitHub, OverlayRepo) in every method: `is_mutable()`, `is_syncable()`, `is_updatable()`, `source_type_label()`, and any others. Verify each match arm is correct. Also review CCL serialization/deserialization -- check for data loss during round-trips. Note sickle crate (v0.1.2) maintenance status.

- `config.rs` (919 lines): CCL parsing correctness. Multi-source config handling. What happens with malformed config files?

- `cache.rs` (1040 lines): **Security focus -- git command construction**. Verify all `Command::new("git")` calls construct arguments safely. Look for string interpolation into git arguments that could allow injection. Check atomic operations -- what happens if clone/checkout is interrupted?

- `sources.rs` (1045 lines): Multi-source priority resolution. First-match-wins semantics -- verify ordering is respected. What happens with duplicate source names?

- `overlay_repo.rs` (1576 lines): **Security focus -- path traversal validation** (`validate_path_component`). URL scheme validation (must allow only HTTPS and SSH). Directory traversal checks with Unicode edge cases.

- `detection.rs` (844 lines): File discovery correctness. Gitignore interaction -- does it correctly identify gitignored files? Edge cases with empty repos, repos without .git directory.

- `selection.rs` (3329 lines): Terminal state cleanup is the key concern. Verify Ctrl+C and panic recovery paths properly restore terminal. Focus on the RAII guard pattern for terminal raw mode. Do NOT review the rendering logic in detail (it is visual, not correctness-critical).

**SourceResolver verification (REVIEW-07) -- specific steps:**
1. Find the SourceResolver trait definition and all its methods
2. Find the implementation block(s)
3. For EACH method, verify all 3 variants (Local, GitHub, OverlayRepo) appear in the match
4. Verify the behavior of each arm is correct per the trait method's semantics
5. Search for any code that matches on OverlaySource directly (outside SourceResolver) and verify it either delegates to SourceResolver or has a legitimate reason to match directly

Search pattern to find direct OverlaySource matching outside SourceResolver:
```
rg "match.*source" src/cli.rs src/lib.rs --context 5
```

**IMPORTANT constraints (same as Task 1):**
- Only fix correctness bugs
- Do NOT refactor
- Document tech debt in SUMMARY
  </action>
  <verify>
    <automated>cargo test --lib -- state config cache sources overlay_repo detection selection 2>&1 | tail -5 && cargo test source_resolver 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 infrastructure/resolution/support modules reviewed. SourceResolver trait verified complete across all OverlaySource variants. Bugs found are fixed with tests passing. Security review of git command construction and path validation complete. Findings documented.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `just check` passes (format + lint + all tests)
2. All 13 modules have been reviewed per the checklist
3. SourceResolver trait implementation verified complete (REVIEW-07)
4. Any bugs found during review are fixed and tests pass
5. No refactoring was performed -- only bug fixes
6. SUMMARY documents all findings: bugs fixed, tech debt noted, security observations
</verification>

<success_criteria>
- All 93+ existing tests pass after any changes
- Leaf modules (overlay_name, fuzzy, json_merge, github, upstream, reference) reviewed and verified correct
- Infrastructure modules (state, config, cache) reviewed with security verification on git commands
- Resolution/support modules (sources, overlay_repo, detection, selection) reviewed with path traversal and terminal cleanup verification
- SourceResolver trait has complete coverage of all 3 OverlaySource variants in every method
- No regressions introduced by any fixes
- `just check` passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-review-and-bug-fixes/01-01-SUMMARY.md`

The SUMMARY must include:
- List of all modules reviewed
- Bugs found and fixed (with descriptions)
- Security observations (git command safety, path validation, flag injection)
- SourceResolver verification results
- Tech debt documented for future phases
- sickle crate maintenance status observation
</output>
