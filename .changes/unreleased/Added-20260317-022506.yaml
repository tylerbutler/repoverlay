kind: Added
body: |-
    Support `--from @library` for explicit library targeting

    `repoverlay apply my-overlay --from @library` resolves exclusively from
    the library. Gives a clear error listing available library overlays when
    the requested overlay is not found, instead of falling through to other
    source resolution.
time: 2026-03-17T02:25:06Z
component: apply
